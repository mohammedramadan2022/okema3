{/* <script>
    $(document).ready(function () {
    let detailsTable = $("#details");
    let detailsRows = "";
    for (let i = 1; i <= 15; i++) { // Simulating more than 10 rows
        if (i === 11) {
            detailsRows += '<tr class="page-break"></tr>'; // Insert a page break
        }
        detailsRows += `<tr><td>Detail Row ${i}</td><td>Data</td><td>Data</td><td>Data</td></tr>`;
    }
    detailsTable.html(detailsRows);
});
</script> */}
